# finance-suite

Một chỗ gom tất cả artifact dự án finance-app.

## Repos
- `finance-manager-app/` — Flutter app
- `finance-api/` — backend API
- `finance-bot/` — Telegram bot (nhập liệu)

## Ops
- APK Drive folder: `gdrive:OpenClaw Outputs/financeapp/`
- Quy ước: "xuất APK" = build release → dọn `*.apk` cũ trên Drive → upload bản mới.
